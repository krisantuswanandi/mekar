{
  "name": "mekar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "turbo": "turbo"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "workspaces": [
    "apps/*"
  ]
}